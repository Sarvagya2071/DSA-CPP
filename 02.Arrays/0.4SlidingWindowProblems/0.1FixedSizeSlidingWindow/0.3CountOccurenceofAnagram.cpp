//Count the Occurence of Anagram

#include<iostream>
#include<map>
#include<list>
using namespace std;

int main(){
    string txt;
    cin>>txt;
    string pat;
    cin>>pat;
    int k = pat.length();
    map<char, int> mpp;
    for(int i = 0; i < k; i++){
        mpp[pat[i]];
    }
    int count = mpp.size();
    int ans = 0;
    int i = 0;
    int j = 0;
    while(j < txt.length()){
        
    }

    return 0;
}